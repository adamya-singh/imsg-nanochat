# nextjs-frontend

Reserved for a later Next.js interface that will talk to trained checkpoints.

Current status:
- This folder exists intentionally, but no frontend implementation is checked in yet.
- The current completed work in this repo is still the reply-only `chat.db` extraction path in `training-data/`.
- Frontend work remains intentionally deferred until data preparation, first training runs, and evaluation prompts/artifacts are in place.
