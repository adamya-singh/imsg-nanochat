# evaluation-prompts

Store fixed prompts, contact-specific probes, and qualitative evaluation notes here.

Current status:
- This folder is reserved but does not yet contain a checked-in prompt set.
- No wrapper-project checkpoint evaluation artifacts are implemented here yet.
- When added, prompts should mirror the same mode-based contract used during training so checkpoint comparisons stay consistent.
