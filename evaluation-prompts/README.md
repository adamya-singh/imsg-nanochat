# evaluation-prompts

Reserved for fixed prompts, future evaluation probes, and qualitative checkpoint notes.

Current status:
- This folder exists intentionally, but no checked-in prompt set or wrapper-project checkpoint evaluation artifacts are implemented yet.
- The current completed work in this repo is still the reply-only, contact-agnostic `chat.db` extraction path in `training-data/`.
- Prompt sets added here should mirror the same mode-based contract used during training so checkpoint comparisons stay consistent.
- This folder remains downstream of the first data-preparation and training loop, which are not complete in the wrapper repo yet.
